import { PageHeader } from "@/components/admin/page-header";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Field, SelectField } from "@/components/ui/field";
import { createWedding } from "@/features/weddings/actions";
import { JAMAICAN_PARISHES } from "@/lib/config/brand";

export default async function NewWeddingPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const params=await searchParams; return <><PageHeader eyebrow="Step 1 of 7" title="Begin a new love story" description="Start with the essential details. Everything saves as a private draft."/><Card className="wizard-card">{params.error && <p className="alert error">{params.error}</p>}<form className="form-grid" action={createWedding}><Field label="Partner one name" name="partnerOneName" required/><Field label="Partner two name" name="partnerTwoName" required/><Field className="span-2" label="Display names" hint="For example: Annette & Clive" name="displayNames" required/><Field label="Wedding date" name="weddingDate" type="date" required/><Field label="Wedding time" name="weddingTime" type="time" required/><SelectField label="Timezone" name="timezone" defaultValue="America/Jamaica"><option>America/Jamaica</option></SelectField><SelectField label="Package" name="package" defaultValue="essential"><option value="essential">Essential</option><option value="signature">Signature</option><option value="bespoke">Bespoke</option></SelectField><Field className="span-2" label="URL slug" hint="Lowercase letters, numbers and hyphens only" name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*"/><Field label="Contact email" name="contactEmail" type="email"/><Field label="Contact phone" name="contactPhone" type="tel"/><div className="form-actions span-2"><Button type="submit">Create draft & continue</Button></div></form><datalist id="parishes">{JAMAICAN_PARISHES.map(p=><option key={p}>{p}</option>)}</datalist></Card></>; }
